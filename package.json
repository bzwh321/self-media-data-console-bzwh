{
  "name": "self-media-data-console",
  "version": "0.1.0",
  "private": false,
  "description": "A portable local self-media dashboard with isolated demo and personal data profiles.",
  "scripts": {
    "start": "python scripts/console_server.py",
    "demo": "python scripts/generate_demo_data.py",
    "check": "python tests/test_portable_setup.py && python scripts/check_public_safety.py",
    "report": "python scripts/daily_pipeline.py --stage report",
    "screenshot": "python scripts/daily_pipeline.py --stage screenshot"
  },
  "devDependencies": {
    "puppeteer": "^25.5.0"
  }
}
